浏览器有同域限制，也就是我们经常会遇到跨域请求资源的问题。
如果我们做的是前端开发，你可以经常会听到关于跨域的内容，比如说使用JSONP实现跨域请求。
那么具体跨域是怎么样的概念呢？

在code中的cors文件目录下，我们创建了两个node服务，
第一个node服务server.js, 监听8888端口。
第二个node服务server2.js，监听8887端口。
其中,8888服务中的test.html页面会向8887服务发送一个ajax请求,那么这个请求就是一个跨域的请求

此节去查看code/cors中代码注释！！！

