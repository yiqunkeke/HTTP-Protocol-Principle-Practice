资源验证
    .如果给Cache-Control设置了 no-cache之后，那么每一次对该资源发起请求时，它都会去服务器端进行资源验证。
     验证完之后，如果确定这个资源可以使用缓存，那么它才会读取本地的缓存。


图示：

    --->浏览器
   |      |
   |      |
  命中    |  创建请求
   |      |
   |      |        未命中             未命中
    ----本地缓存------------>代理缓存------------>源服务器



    . 那么我们的数据如何进行验证呢？

        在http协议里面主要有两个验证的头，一个是 Last-Modified， 一个是Etag

        . Last-Modified----上次修改时间
          主要配合If-Modified-Since或者 If-Unmodified-Since使用
          对比上次修改时间以验证资源是否需要更新

        . Etag---是一个更加严格的验证  
           数据签名-----我们的资源，对于它的内容，产生唯一的签名。
           也就是说，如果资源进行过修改，那么它的签名就会变成一个新的，只要有任何修改，它的两个签名就会不一样
           最典型的做法就是：对它的内容进行一个哈希计算。

           配合If-Match或者 If-None-Match使用
           对比资源的签名判断是否使用缓存
           


                               