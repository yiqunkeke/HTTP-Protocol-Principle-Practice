. HTTP/0.9
    . 只有一个命令GET。
      我们现在经常用到的GET请求、POST请求，都是http的命令，或者叫http的方法。
      后面章节中会详细讲解关于命令的内容。

    . 没有HEADER等描述数据的信息。

    . 服务器发送完毕，就关闭TCP连接。
      这里大家要注意一点，这里提到的TCP连接和我们讲的HTTP请求是不一样的。
      如果大家做过web开发，大家应该会听到一些“http请求的并发数”这种术语的描述，
      TCP连接和HTTP请求是不同的概念，在同一个TCP连接里面，可以有很多个HTTP请求